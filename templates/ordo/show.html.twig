{% extends 'base.html.twig' %}

{% block title %}Ordo{% endblock %}

{% block body %}
    <h1>Ordo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordo.id }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ ordo.file }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ordo_index') }}">back to list</a>

    <a href="{{ path('ordo_edit', {'id': ordo.id}) }}">edit</a>

    {{ include('ordo/_delete_form.html.twig') }}
{% endblock %}
